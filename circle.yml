test:
  override:
    - bundle exec rubocop
    - bundle exec rspec
  post:
    - git fetch --unshallow
    - git remote add gitlab git@gitlab.doc.ic.ac.uk:cowhub/cowhub-server-api
    - git push gitlab $(git branch | grep \* | awk -F ' ' '{ print $2 }')
