{
  "name": "cowhub-server-api",
  "version": "0.0.1",
  "scripts": {
    "server": "env RAILS_ENV=development bundle exec rails s",
    "test": "env RAILS_ENV=test bundle exec rubocop; env RAILS_ENV=test bundle exec rspec" 
  }
}
