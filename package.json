{
  "name": "cowhub-server-api",
  "version": "0.0.1",
  "scripts": {
    "db_setup": "bundle exec rake db:setup",
    "db_migrate": "bundle exec rake db:migrate",
    "rspec": "npm run db_migrate; bundle exec rspec",
    "rubocop": "bundle exec rubocop",
    "test": "npm run rubocop -s; npm run rspec -s"
  }
}
